spring:
  datasource:
    url: jdbc:h2:mem:testdb
    data: classpath:data.sql

  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format-sql: true

jwt:
  secret: TEST1111222233334444555566667777
  expiration:
    access: 5
    refresh: 10080
